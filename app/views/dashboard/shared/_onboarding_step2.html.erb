<div class="onboarding">
  <img src="/images/welcome_1.png" alt="">
  <h3>Sie Sind fast fertig!</h3>
  <div class="newEvent newEvent1" style="width: calc(100% - 20px); max-width: 900px; text-align: center; overflow: scroll">
    
    <p>Kopieren Sie den Code-Schnipsel in den &lt;head&gt;-Bereich Ihrer Webseite und tragen Sie danach Ihre Webseite ein</p>
    <code>
        &lt;script src="https://radr.herokuapp.com/api/users/<%= @user.id %>.js"&gt;&lt;/script&gt;
    </code>
    <%= form_for(@user, :html => {role: "form"}, url: update_user_path) do |f| %>
        <fieldset>
        <div class="row">
          <label for="website">Webseite</label>
          <%= f.text_field :website, placeholder: "beispiel.de" %>
        </div>
        <div class="row">
            <%= f.submit "Erstellen", id: "find", style: "margin-top: 10px" %>
        </div>
        </fieldset>
    <% end %>
  </div>
</div>