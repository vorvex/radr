<% @user.events.upcoming.each do |event| %>
  <div style="display: flex; align-items: center">
    <img src="<%= event.image_url %>" alt="">
    <div>
      <h3><%= event.name %></h3>
      <p><%= event.description %></p>
      <p><%= event.start_time.strftime("%d.%m um %H:%M") %> im <%= event.place.name %></p>
      
    </div>
    <div>
      <a href="">Bearbeiten</a>
      <a href="">Löschen</a>
    </div>
  </div>
<% end %>